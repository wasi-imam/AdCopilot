[pytest]
testpaths = tests

addopts =
    -v
    --cov=scoring
    --cov=rag
    --cov=agents
    --cov=utils
    --cov-report=term-missing
    --cov-report=html:htmlcov

filterwarnings =
    ignore::DeprecationWarning
